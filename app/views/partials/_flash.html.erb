<% flash.each do |name, msg| %>
  <div data-alert data-options="animation_speed:500, animation:'fadeOut';" class="alert-box <%= flash[:notice] ? "success" : "warning" %> radius">
  	<%= msg %>
    <a href="#" class="close">&times;</a>
    
  </div>
<% end %>